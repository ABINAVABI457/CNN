# Histopathologic Cancer Detection using CNN

## 📌 Project Overview
This project applies Deep Learning to medical imaging. I built and trained a Convolutional Neural Network (CNN) to identify metastatic cancer in small image patches taken from larger digital pathology scans. 

## 🛠️ Tech Stack
* **Language:** Python
* **Libraries:** TensorFlow/Keras, NumPy, Pandas, Matplotlib
* **Environment:** Jupyter Notebook

## 📊 The Dataset
* The model was trained on [Insert dataset name, e.g., the PatchCamelyon (PCam) dataset from Kaggle].
* It consists of [Insert number] of histopathologic images.

## 🚀 Model Architecture & Results
* Engineered a CNN utilizing [mention any specific layers, like Conv2D, MaxPooling, Dropout].
* Achieved an accuracy of **[Insert your accuracy percentage, e.g., 92%]** on the validation set.
